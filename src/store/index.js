import { createStore } from 'vuex'
import wallet from '@/store/wallet/wallet.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    wallet: wallet,
  }
})
