/**
 * BetCoffee API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BetOutcome from './BetOutcome';

/**
 * The SportMatchBet model module.
 * @module model/SportMatchBet
 * @version 0.1.0
 */
class SportMatchBet {
    /**
     * Constructs a new <code>SportMatchBet</code>.
     * @alias module:model/SportMatchBet
     * @param user_id {Number} 
     * @param match_id {Number} 
     * @param bet_amount {Number} 
     * @param bet_team {module:model/BetOutcome} 
     * @param bet_ratio {Number} 
     * @param free_bet_id {Number} 
     */
    constructor(user_id, match_id, bet_amount, bet_team, bet_ratio, free_bet_id) { 
        
        SportMatchBet.initialize(this, user_id, match_id, bet_amount, bet_team, bet_ratio, free_bet_id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, user_id, match_id, bet_amount, bet_team, bet_ratio, free_bet_id) { 
        obj['user_id'] = user_id;
        obj['match_id'] = match_id;
        obj['bet_amount'] = bet_amount;
        obj['bet_team'] = bet_team;
        obj['bet_ratio'] = bet_ratio;
        obj['free_bet_id'] = free_bet_id;
    }

    /**
     * Constructs a <code>SportMatchBet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SportMatchBet} obj Optional instance to populate.
     * @return {module:model/SportMatchBet} The populated <code>SportMatchBet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SportMatchBet();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'Number');
            }
            if (data.hasOwnProperty('match_id')) {
                obj['match_id'] = ApiClient.convertToType(data['match_id'], 'Number');
            }
            if (data.hasOwnProperty('bet_amount')) {
                obj['bet_amount'] = ApiClient.convertToType(data['bet_amount'], 'Number');
            }
            if (data.hasOwnProperty('bet_team')) {
                obj['bet_team'] = BetOutcome.constructFromObject(data['bet_team']);
            }
            if (data.hasOwnProperty('bet_ratio')) {
                obj['bet_ratio'] = ApiClient.convertToType(data['bet_ratio'], 'Number');
            }
            if (data.hasOwnProperty('tx_id')) {
                obj['tx_id'] = ApiClient.convertToType(data['tx_id'], 'String');
            }
            if (data.hasOwnProperty('win_amount')) {
                obj['win_amount'] = ApiClient.convertToType(data['win_amount'], 'Number');
            }
            if (data.hasOwnProperty('free_bet_id')) {
                obj['free_bet_id'] = ApiClient.convertToType(data['free_bet_id'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SportMatchBet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SportMatchBet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SportMatchBet.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['tx_id'] && !(typeof data['tx_id'] === 'string' || data['tx_id'] instanceof String)) {
            throw new Error("Expected the field `tx_id` to be a primitive type in the JSON string but got " + data['tx_id']);
        }

        return true;
    }


}

SportMatchBet.RequiredProperties = ["user_id", "match_id", "bet_amount", "bet_team", "bet_ratio", "free_bet_id"];

/**
 * @member {Number} user_id
 */
SportMatchBet.prototype['user_id'] = undefined;

/**
 * @member {Number} match_id
 */
SportMatchBet.prototype['match_id'] = undefined;

/**
 * @member {Number} bet_amount
 */
SportMatchBet.prototype['bet_amount'] = undefined;

/**
 * @member {module:model/BetOutcome} bet_team
 */
SportMatchBet.prototype['bet_team'] = undefined;

/**
 * @member {Number} bet_ratio
 */
SportMatchBet.prototype['bet_ratio'] = undefined;

/**
 * @member {String} tx_id
 */
SportMatchBet.prototype['tx_id'] = undefined;

/**
 * @member {Number} win_amount
 */
SportMatchBet.prototype['win_amount'] = undefined;

/**
 * @member {Number} free_bet_id
 */
SportMatchBet.prototype['free_bet_id'] = undefined;

/**
 * @member {Number} id
 */
SportMatchBet.prototype['id'] = undefined;






export default SportMatchBet;

